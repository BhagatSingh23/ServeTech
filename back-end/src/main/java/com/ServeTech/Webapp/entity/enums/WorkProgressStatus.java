package com.ServeTech.Webapp.entity.enums;

// This will help determine the schedule of the work and worker if they are usy or free
public enum WorkProgressStatus {
    NOT_STARTED,
    IN_PROGRESS,
    ON_HOLD,
    COMPLETED,
    CANCELLED
}